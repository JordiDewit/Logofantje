import { MateriaalFilterPipe } from './materiaal-filter.pipe';

describe('MateriaalFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new MateriaalFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
